command publish do
	# Just in case - remove first:
	echo "locked"
	exit 1

	from "./ss-web-utils" do
		try cargo publish
	from "./ss-css-types" do
		try cargo publish
	from "./ss-cssom-tree" do
		try cargo publish
	from "./ss-trees" do
		try cargo publish
	from "./ss-view-tree" do
		try cargo publish
	from "./ss-dom-tree" do
		try cargo publish
	from "./subscript" do
		try cargo publish
