Note that `./nav-app` currently showcases navigation, components, and subscriptions.
It's currently perhaps the best example feature-wise.

# Building WASM code (in general):
.1 `npm install && npm start`